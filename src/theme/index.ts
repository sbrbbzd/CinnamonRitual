import { colors } from './colors';
import { fonts, spacing, borderRadius } from './fonts';

export const theme = {
    colors,
    fonts,
    spacing,
    borderRadius,
};

export type Theme = typeof theme;
